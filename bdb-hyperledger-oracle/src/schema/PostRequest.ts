export default 
{
    "type": "object",
    "required": ["query", "callback"],
    "properties": {
        "query": {
            "type": "string"
        },
        "callback": {
            "type": "string"
        }
    }
}

